name: Greet
description: Print a greeting using a custom JavaScript action

inputs:
  name:
    description: Name to greet
    required: true

outputs:
  message:
    description: Greeting message

runs:
  using: node24
  main: index.js
